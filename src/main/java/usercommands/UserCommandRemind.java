package usercommands;

import utilities.Command;

/**
 * Concrete class that encapsulates information about the REMIND command
 */
public class UserCommandRemind extends UserCommand {
    public UserCommandRemind() {
        super(Command.REMIND);
    }
}
