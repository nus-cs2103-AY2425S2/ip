package chillguy.enums;

/**
 * Represents the different types of errors that can occur in the system.
 * <p>
 * The {@code ErrorType} enum defines various error types that the system may encounter:
 */
public enum ErrorType {
    COMMAND_ERROR,
    CREATE_FILE_ERROR,
    DATE_ERROR,
    DEADLINE_ERROR,
    DELETE_ERROR,
    EVENT_ERROR,
    READ_FORMAT_ERROR,
    READ_TYPE_ERROR,
    LIST_WITH_DATE_ERROR,
    LIST_WITH_KEYWORD_ERROR,
    MARK_ERROR,
    READ_FILE_ERROR,
    TODO_ERROR,
    UNMARK_ERROR,
    NO_KEYWORD_ERROR,
    TYPE_ERROR,
    REMINDERS_ERROR
}

