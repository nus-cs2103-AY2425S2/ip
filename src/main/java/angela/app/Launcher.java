package angela.app;

import javafx.application.Application;

/**
 * Main class for GUI.
 */
public class Launcher {
    /**
     * Main method to call for running the GUI.
     * @param args
     */
    public static void main(String[] args) {
        Application.launch(App.class, args);
    }
}
