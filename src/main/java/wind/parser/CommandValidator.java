package wind.parser;

/**
 * This class has been deprecated as validation is now handled by regex patterns in Parser.java.
 * Keeping the class for potential future validation needs beyond regex pattern matching.
 */
public class CommandValidator {
    // All validation logic has been moved to regex patterns in Parser.java
}
