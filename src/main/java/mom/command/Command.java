package mom.command;

/**
 * Command data type containing all valid commands for the chatbot.
 */
public enum Command {
    regular, bye, list, mark, unmark, delete, todo, deadline, event, find, undo
}
