package astraea.command;

/**
 * Contains all the different types of valid commands that a user can issue.
 */
public enum CommandType {
    LIST, MARK, UNMARK, TODO, DEADLINE, EVENT, DELETE, FIND, ADD_ALIAS, REMOVE_ALIAS, EXIT
}
