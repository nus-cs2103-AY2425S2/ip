package utilities;

/**
 * Enum that contains the types of commands the user can input
 */
public enum Command {
    LIST,
    MARK,
    UNMARK,
    TODO,
    DEADLINE,
    EVENT,
    DELETE,
    FIND,
    REMIND,
}
